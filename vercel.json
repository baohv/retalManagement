{
  "framework": "nextjs",
  "buildCommand": "cp prisma/schema.postgres.prisma prisma/schema.prisma && npx prisma generate && npx next build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  },
  "crons": [
    {
      "path": "/api/reset-db",
      "schedule": "0 0 * * *"
    }
  ]
}